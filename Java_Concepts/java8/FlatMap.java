package java8;

import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;

public class FlatMap {
    public static void main(String[] args) {
//        Given a List<Department>, where each Department
//        contains a List<Employee>
//        , write a Java 8 program
//        to get a List<Employee> using flatMap().

// List<Employee> allEmp=    depart.stream().flatMap(dept->dept.getEmployees().stream).collect(Collectors.toList());
//        System.out.println(allEmp);

//        Given a List<Student>, where each Student
//        has a List<String> of subjects,
//        return a List<String> containing all subjects.

//        List<String> allSub= student.stream().
//                flatMap(student.getSubject().stream()).distinct().collect(Collectors.toList());

//        Return a list of all employees sorted in ascending order of salary.

//        depart.stream().flatMap(dept->dept.getEmployees().stream).sorted(Comparator.comparingInt(emp->emp.salry)).collect(...tolistl)

    
    }
}
